import React, {Component} from 'react';

class Header extends Component {
render() {
  return(
    <header>
      	<h1 tabIndex ={0} className = "title"> Athens is the new Berlin</h1>
  	</header>
  );

}
}

export default Header;